targets:
  $default:
    builders:
      json_serializable:
        options:
          checked: true
          explicit_to_json: true
      retrofit_generator:
        options:
          format_output: false
